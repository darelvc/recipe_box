<div id="disqus_thread"></div>
<script>
	var disqus_config = function () {
		this.page.url = '<%= url_for([@recipe, {only_path: false}]) %>'; // Replace PAGE_URL with your page's canonical URL variable
		this.page.identifier = '<%= @recipe.id %>'; // Replace PAGE_IDENTIFIER with your page's unique identifier variable
	};

/*	var disqus_shortname = 'recipeboxsite';
	var disqus_identifier = '<%= @recipe.id %>';
	var disqus_title = 	'<%= @recipe.title %>';
	var disqus_url = '<%= url_for([@recipe, {only_path: false}]) %>';*/

	(function() { // DON'T EDIT BELOW THIS LINE
		var d = document, s = d.createElement('script');

		s.src = '//recipeboxsite.disqus.com/embed.js';

		s.setAttribute('data-timestamp', +new Date());
		(d.head || d.body).appendChild(s);
	})();
</script>